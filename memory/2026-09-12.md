# 2026-09-12 — Backfill mapping: Neel Chhabra (Resight)

## What was added

`backfill_mappings.json` gains a second entry, `Neel Chhabra`
(`Label_1703214449706674033`), pointing at `https://resight.substack.com/archive`.

`mode: "json"` for the usual Substack reason: the probe found 9 articles in static HTML —
two of which are the `/podcast` and `/notes` tabs, not posts — and warned that the rest is
client-rendered. `/api/v1/archive` returns all 7 posts in one page. Selectors verified
against a real post with lxml/cssselect (the project's parser, not bs4):
`div.available-content` → 52,907 chars, `time[datetime]` → ISO date.

`title_selector` is `h1.post-title`, not the `h1` used in the Joan Westenberg entry.
This publication renders **two** `h1`s and the first is the masthead, "Resight by Neel
Chhabra". It is dead code in `json` mode — `extractor.py:195` is
`title = ref.title or _extract_title(...)` and the listing always supplies a title — but a
selector that silently resolves to the wrong element is not worth keeping. `post-title` is
a stable Substack class; the sibling `title-X77sOw` is hashed and would not survive a
redesign.

## The real finding: this label spans two archives

The sender timeline gives it away:

| Sender | Range | Count |
|---|---|---|
| `neelchhabra@substack.com` ("Neel's Newsletter") | 2025-12-18 → 2026-05-09 | 20 |
| `resight@substack.com` ("Resight") | 2026-08-22 → 2026-09-10 | 4 |

The publication rebranded, and **the back catalogue did not move**.
`neelchhabra.substack.com` still holds 23 posts (2025-04-28 → 2026-05-09);
`resight.substack.com` starts at 2026-07-20 with 7. Joan Westenberg set the wrong
expectation — her Substack import pulled the whole history into the new home, so one
`archive_url` covered everything. That was luck, not the rule.

Nothing in the scan flags this. `7 listed, 3 already held, 4 missing` is what a healthy
small archive looks like, and all three matches were exact normalised-title hits, so the
matcher confirms the `label_id` is right. Every check in the skill's step 5 passes.

~3 posts from the old publication are missing from the corpus (2025-04-28, 2025-10-12,
2025-12-09 — the pre-subscription back catalogue). Recovering them needs a decision, not a
silent fix: the schema holds one `archive_url` per label and is keyed by label name, so the
old archive cannot be expressed as a second entry without filing its articles into a
different `newsletters/` directory. **Left for the user.**

## Backfilled

4 articles written, 0 failed, `--delay 3` (the Joan Westenberg run hit a 429 without it):
`from-minutes-to-bytes-to-tokens` (2026-07-20), `the-middle-velocity…` (2026-08-01),
`urban-company…` (2026-08-08), `resight-tell-the-node-delhivery-wont-touch` (2026-08-14).
Bodies 8.8k–40k chars, none paywalled, raw HTML self-contained with inlined
`ARTICLE_PAGE_CSS`, no subscribe chrome. Front matter carries the right label, sender,
`source_url` and `origin: "backfill"`.

**Not yet run downstream:** `ingestor-tools` organize and `newsletters-web` build. The four
articles are in `../output` but not in `../newsletters/Neel Chhabra/`.

## Files

Modified: `backfill_mappings.json` (new entry), `LEARNINGS.md` (new section — a rebrand can
split one label across two archives), `.claude/skills/backfill-mapping/SKILL.md` (step 1
gains the sender-timeline query, which is what actually catches this), `PROMPT.md`.

No code changed. 235 tests pass, including the one asserting `gmail_ingestor.db` is
byte-identical after a run.
